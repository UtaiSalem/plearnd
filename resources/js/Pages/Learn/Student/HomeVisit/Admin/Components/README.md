# Home Visit Admin Components

## สถานะการพัฒนา

ฟีเจอร์การจัดการการเยี่ยมบ้านนักเรียนยังอยู่ระหว่างการพัฒนา โดยได้แยก component ออกมาเพื่อให้สามารถกลับมาพัฒนาต่อในอนาคต

## Components

### StudentManagement.vue

**สถานะ**: 🚧 กำลังพัฒนา (Development Paused)

Component สำหรับจัดการข้อมูลนักเรียนในระบบการเยี่ยมบ้าน

#### ฟีเจอร์ที่วางแผนไว้:

##### 1. การจัดการข้อมูลนักเรียน

-   ✅ แสดงสถิตินักเรียน (จำนวนทั้งหมด, กำลังเรียน, รอการเยี่ยมบ้าน, อัตราการเยี่ยมบ้าน)
-   ⏳ ตารางแสดงรายชื่อนักเรียน
-   ⏳ ค้นหานักเรียน (ชื่อ, รหัสนักเรียน, อีเมล)
-   ⏳ กรองข้อมูล (ชั้นเรียน, สถานะการเรียน, สถานะการเยี่ยมบ้าน)
-   ⏳ Pagination

##### 2. CRUD Operations

-   ⏳ เพิ่มนักเรียนใหม่
-   ⏳ แก้ไขข้อมูลนักเรียน
-   ⏳ ลบนักเรียน
-   ⏳ ดูรายละเอียดนักเรียน

##### 3. การนำเข้า/ส่งออกข้อมูล

-   ⏳ นำเข้าข้อมูลจาก Excel (.xlsx, .xls)
-   ⏳ นำเข้าข้อมูลจาก CSV
-   ⏳ ส่งออกข้อมูลเป็น Excel
-   ⏳ ดาวน์โหลด Template สำหรับการนำเข้าข้อมูล

##### 4. ประวัติการเยี่ยมบ้าน

-   ⏳ ดูประวัติการเยี่ยมบ้านของนักเรียน
-   ⏳ แสดงรายละเอียดการเยี่ยมบ้านแต่ละครั้ง
-   ⏳ สรุปสถิติการเยี่ยมบ้าน

##### 5. Bulk Operations

-   ⏳ เลือกนักเรียนหลายคนพร้อมกัน
-   ⏳ นัดหมายเยี่ยมบ้านแบบกลุ่ม
-   ⏳ ส่งออกข้อมูลนักเรียนที่เลือก
-   ⏳ ลบนักเรียนแบบกลุ่ม

#### Props:

```javascript
{
  students: {
    type: Array,
    default: () => []
  }
}
```

#### Events:

```javascript
// To be implemented
emit("student-updated", student);
emit("student-deleted", studentId);
emit("student-added", student);
emit("visit-assigned", { studentIds, visitData });
```

#### Data Structure:

##### Student Object:

```javascript
{
  id: Number,
  student_id: String,        // รหัสนักเรียน
  first_name: String,         // ชื่อ
  last_name: String,          // นามสกุล
  nickname: String,           // ชื่อเล่น
  email: String,              // อีเมล
  phone: String,              // โทรศัพท์
  classroom: String,          // ชั้นเรียน (ป.1-6, ม.1-3)
  status: String,             // สถานะ: active, graduated, transferred, suspended
  visit_status: String,       // สถานะการเยี่ยมบ้าน: pending, in-progress, completed, cancelled
  last_visit_date: Date,      // วันที่เยี่ยมบ้านล่าสุด
  avatar: String              // รูปภาพ
}
```

##### Visit History Object:

```javascript
{
  id: Number,
  student_id: Number,
  visit_date: Date,           // วันที่เยี่ยมบ้าน
  teacher_name: String,       // ชื่อครูผู้เยี่ยม
  teacher_id: Number,
  status: String,             // สถานะ: pending, in-progress, completed, cancelled
  notes: String,              // บันทึกการเยี่ยมบ้าน
  attachments: Array,         // ไฟล์แนบ (รูปภาพ, เอกสาร)
  created_at: Date,
  updated_at: Date
}
```

## โครงสร้างไฟล์

```
Components/
├── README.md                           # เอกสารนี้
├── StudentManagement.vue               # Component หลักสำหรับจัดการนักเรียน (🚧)
├── StudentTable.vue                    # ตารางแสดงรายชื่อนักเรียน (TODO)
├── StudentForm.vue                     # ฟอร์มเพิ่ม/แก้ไขนักเรียน (TODO)
├── StudentImportModal.vue              # Modal นำเข้าข้อมูล (TODO)
├── StudentVisitHistory.vue             # ประวัติการเยี่ยมบ้าน (TODO)
└── StudentFilters.vue                  # ส่วนค้นหาและกรองข้อมูล (TODO)
```

## การใช้งาน

### ในไฟล์ Dashboard.vue:

```vue
<template>
    <div v-if="activeTab === 'students'">
        <StudentManagement :students="studentsData" />
    </div>
</template>

<script>
import StudentManagement from "./Components/StudentManagement.vue";

export default {
    components: {
        StudentManagement,
    },
};
</script>
```

## TODO: แผนการพัฒนาต่อ

### Phase 1: Core Features (เฟสที่ 1)

-   [ ] สร้าง StudentTable component
-   [ ] สร้าง StudentForm component สำหรับ Add/Edit
-   [ ] เชื่อมต่อกับ API backend
-   [ ] ทำระบบ CRUD พื้นฐาน
-   [ ] เพิ่มระบบ pagination และ sorting

### Phase 2: Advanced Features (เฟสที่ 2)

-   [ ] สร้าง StudentImportModal component
-   [ ] ทำระบบนำเข้าข้อมูลจาก Excel/CSV
-   [ ] สร้าง StudentVisitHistory component
-   [ ] ทำระบบส่งออกข้อมูล
-   [ ] เพิ่ม Bulk operations

### Phase 3: Enhancement (เฟสที่ 3)

-   [ ] เพิ่มระบบแจ้งเตือน (notifications)
-   [ ] ทำระบบ validation ที่ดีขึ้น
-   [ ] เพิ่มการแสดงผล responsive ที่ดีขึ้น
-   [ ] เพิ่ม loading states และ error handling
-   [ ] เพิ่ม unit tests

## Notes สำหรับนักพัฒนา

1. **Code ที่ถูก comment ไว้**: ใน Dashboard.vue มี code ที่ถูก comment ไว้ทั้งหมดอยู่ รอย้ายไปใช้ใน component ใหม่

2. **API Endpoints ที่ต้องเตรียม**:

    - `GET /api/home-visit/students` - ดึงรายชื่อนักเรียนทั้งหมด
    - `POST /api/home-visit/students` - เพิ่มนักเรียนใหม่
    - `PUT /api/home-visit/students/{id}` - แก้ไขข้อมูลนักเรียน
    - `DELETE /api/home-visit/students/{id}` - ลบนักเรียน
    - `GET /api/home-visit/students/{id}/visits` - ดูประวัติการเยี่ยมบ้าน
    - `POST /api/home-visit/students/import` - นำเข้าข้อมูล
    - `GET /api/home-visit/students/export` - ส่งออกข้อมูล

3. **Dependencies ที่อาจต้องเพิ่ม**:

    - `xlsx` - สำหรับการจัดการไฟล์ Excel
    - `papaparse` - สำหรับการจัดการไฟล์ CSV
    - `vue-multiselect` - สำหรับ bulk selection ที่ดีขึ้น

4. **State Management**: ควรพิจารณาใช้ Pinia store สำหรับจัดการ state ของนักเรียนและการเยี่ยมบ้าน

## อ้างอิง

-   [Vue 3 Documentation](https://vuejs.org/)
-   [Inertia.js Documentation](https://inertiajs.com/)
-   [Tailwind CSS Documentation](https://tailwindcss.com/)

---

**สถานะล่าสุด**: ปิดการพัฒนาชั่วคราว (18 พฤศจิกายน 2025)  
**ผู้พัฒนา**: UtaiSalem  
**Repository**: plearnd
