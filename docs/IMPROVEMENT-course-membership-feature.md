# การปรับปรุงฟีเจอร์การสมัครเข้าร่วมรายวิชา

## วันที่: 3 พฤศจิกายน 2025

## สรุปการปรับปรุง

ได้ทำการปรับปรุงฟีเจอร์การสมัครเข้าร่วมรายวิชาใน `CourseProfileCover.vue` เพื่อเพิ่มประสบการณ์ผู้ใช้และความเสถียรของระบบ โดยใช้แนวทาง **Server-Side Rendering (SSR)** สำหรับข้อมูลกลุ่ม

## แนวคิดหลัก

### ปัญหาเดิม

-   เมื่อกดปุ่ม "ขอเป็นสมาชิก" ระบบจะพยายามเรียกข้อมูลรายการกลุ่ม (Course Groups) ผ่าน Pinia Store ทันที
-   อาจทำให้เกิดความล่าช้าหรือข้อผิดพลาด รายการไม่ปรากฏ
-   มีการ fetch ข้อมูลซ้ำซ้อนทุกครั้งที่เปิด dropdown

### แนวทางแก้ไขใหม่

1. **โหลดข้อมูลล่วงหน้า (Pre-loading)**: Backend (Controller) ส่งข้อมูลรายการกลุ่มทั้งหมดมาพร้อมกับหน้าเพจตั้งแต่แรกผ่าน Inertia.js Props
2. **แสดงข้อมูลทันที (Instant Display)**: เมื่อผู้ใช้กดปุ่ม "ขอเป็นสมาชิก" ให้ดึงข้อมูลจาก `usePage()` มาแสดงผลทันที
3. **เรียก Action เมื่อเลือก (Action on Selection)**: จะเรียก Pinia Store เพื่อส่งคำขอก็ต่อเมื่อผู้ใช้คลิกเลือกกลุ่มจริงๆ

### ข้อดี

-   ⚡ **Performance ดีขึ้น** - ไม่ต้อง fetch ข้อมูลซ้ำทุกครั้ง
-   🎯 **UX ดีขึ้น** - แสดงรายการได้ทันที ไม่มี loading delay
-   🚀 **Server-Side Rendering** - ข้อมูลมาพร้อม page load
-   📉 **ลด API Calls** - ประหยัด bandwidth และลด server load
-   💾 **Offline-friendly** - สามารถดูรายการได้แม้ connection ไม่ดี

## รายละเอียดการปรับปรุง

### 1. Data Management Improvements

#### การจัดการข้อมูลกลุ่ม

-   **เปลี่ยนจาก reactive variable เป็น computed property**:

    ```javascript
    // Before
    const courseGroups = usePage().props.courseGroups || [];

    // After - Reactive to Inertia props changes
    const courseGroups = computed(() => usePage().props.courseGroups || []);
    ```

-   **เพิ่ม helper computed property**:
    ```javascript
    const hasGroups = computed(
        () => courseGroups.value && courseGroups.value.length > 0
    );
    ```

#### ข้อมูลที่ Backend ต้องส่งมา

```php
// Laravel Controller Example
return Inertia::render('Courses/Show', [
    'course' => $course,
    'courseGroups' => $course->groups()
        ->with(['members']) // For members_count
        ->orderBy('name')
        ->get()
        ->map(fn($group) => [
            'id' => $group->id,
            'name' => $group->name,
            'image' => $group->image_url,
            'members_count' => $group->members_count, // Optional
        ]),
    'isCourseAdmin' => auth()->user()?->canManageCourse($course),
    'courseMemberOfAuth' => $course->members()
        ->where('user_id', auth()->id())
        ->first(),
]);
```

### 2. UI/UX Improvements

#### ปุ่มและ Dropdown

-   **เพิ่ม icons ที่มีความหมาย**:

    -   `heroicons-outline:user-add` สำหรับปุ่มขอเป็นสมาชิก
    -   `heroicons:clock` สำหรับสถานะรอการตอบรับ
    -   `heroicons:chevron-down` สำหรับ dropdown indicator
    -   `heroicons:x-circle` สำหรับยกเลิกคำขอ
    -   `majesticons:door-exit-line` สำหรับออกจากสมาชิก

-   **ปรับปรุง transitions และ animations**:

    -   เพิ่ม smooth transitions สำหรับการเปิด/ปิด dropdown
    -   เพิ่ม transform animations (translate, opacity) สำหรับ dropdown
    -   เพิ่ม rotate animation สำหรับ chevron icon
    -   เพิ่ม shadow effects และ hover states

-   **ปรับปรุง responsive design**:
    -   Desktop version: hover effects
    -   Mobile version: active states สำหรับ touch interactions
    -   ปรับ padding และ spacing ให้เหมาะสมกับทั้ง desktop และ mobile

#### Dropdown Enhancements

-   **เพิ่ม Header ใน Dropdown**: แสดงข้อความแนะนำ "เลือกกลุ่มที่ต้องการเข้าร่วม"
-   **เพิ่ม max-height และ overflow**: รองรับกรณีที่มีกลุ่มจำนวนมาก (max-h-64 overflow-y-auto)
-   **แสดงจำนวนกลุ่ม**: แสดงจำนวนกลุ่มทั้งหมดที่ปุ่ม เช่น "ขอเป็นสมาชิก (5)"
-   **แสดงจำนวนสมาชิก**: แสดงจำนวนสมาชิกในแต่ละกลุ่ม (ถ้ามีข้อมูล `members_count`)

#### รูปภาพกลุ่ม

-   เพิ่ม border สำหรับรูปภาพกลุ่มเพื่อให้ดูชัดเจนขึ้น
-   เพิ่มการแสดง icon fallback เมื่อไม่มีรูปภาพ
-   ใช้ flexbox layout เพื่อจัดการ truncate ชื่อกลุ่มที่ยาว
-   เพิ่ม flex-shrink-0 สำหรับ icon และรูปภาพเพื่อไม่ให้ถูกบีบ

### 2. Functionality Improvements

#### การจัดการ Dropdown

-   **Click outside to close**: เพิ่ม event listener เพื่อปิด dropdown เมื่อคลิกนอกพื้นที่
-   **Escape key support**: กด Escape key เพื่อปิด dropdown ทั้งหมด
-   **Auto-close after action**: dropdown จะปิดอัตโนมัติหลังจากเลือกตัวเลือก

#### การป้องกันการกระทำซ้ำ

-   เพิ่มการตรวจสอบ `isRequestingMember` และ `isRequestingUnmember` เพื่อป้องกันการส่งคำขอซ้ำ
-   Disable buttons ขณะกำลังประมวลผล
-   แสดง loading spinner เมื่อกำลังประมวลผล

#### Confirmation Dialog

-   เพิ่ม confirmation dialog เมื่อผู้ใช้ต้องการออกจากรายวิชา (สมาชิกที่ได้รับการอนุมัติแล้ว)
-   ไม่แสดง confirmation สำหรับการยกเลิกคำขอที่รอการอนุมัติ

### 3. Store Enhancements

#### Error Handling

-   ปรับปรุงการจัดการ error messages
-   แสดง error message จาก server response
-   Fallback error messages สำหรับกรณีที่ไม่มี error message จาก server

#### Toast Notifications

-   เพิ่มการแสดง success message เมื่อดำเนินการสำเร็จ
-   เพิ่มการแสดง error message เมื่อเกิดข้อผิดพลาด
-   รองรับ global `window.$toast` object

#### State Management

-   เพิ่ม watch สำหรับ `isRequestingMember` เพื่อปิด dropdown อัตโนมัติ
-   ปรับปรุงการ cleanup state เมื่อ component ถูก unmount

### 4. Code Quality

#### Consistency

-   ใช้ `group.id` เป็น key แทน `index` ใน v-for
-   ปรับปรุงการตั้งชื่อ function parameters (groupIndx -> groupIdx)
-   เพิ่ม null check และ optional chaining

#### Performance

-   ใช้ computed properties จาก store
-   จัดการ event listeners อย่างถูกต้อง (cleanup on unmount)
-   ใช้ refs สำหรับ DOM elements แทนการ query selector

### 5. Code Quality & Cleanup

#### Removed Unused Code

-   ลบ import `CyanSeaButton` ที่ไม่ได้ใช้งาน
-   ลบ console.log debug statements
-   ลบ setTimeout ที่ไม่จำเป็น

#### Improved Conditionals

-   ใช้ `hasGroups` computed property แทนการเช็ค `!courseGroups || courseGroups.length==0` ซ้ำๆ
-   ทำให้โค้ดอ่านง่ายและ maintain ง่ายขึ้น

### 6. Accessibility

-   เพิ่ม keyboard support (Escape key)
-   เพิ่ม disabled states ที่ชัดเจน
-   เพิ่ม cursor styles (cursor-pointer, cursor-not-allowed)
-   เพิ่ม visual feedback สำหรับทุก action
-   ใช้ semantic HTML และ ARIA attributes ที่เหมาะสม

## การใช้งาน

### ปุ่มขอเป็นสมาชิก

```vue
<!-- กรณีไม่มีกลุ่ม -->
<button @click="onRequestToBeMember(null, null)">
  ขอเป็นสมาชิก
</button>

<!-- กรณีมีกลุ่ม -->
<button @click="onRequestToBeMember(groupId, groupIndex)">
  เข้าร่วมกลุ่ม {{ groupName }}
</button>
```

### ปุ่มออกจากสมาชิก

```vue
<button @click="onRequestToBeUnMember">
  ออกจากสมาชิก
</button>
```

### การใช้งาน Store

```javascript
// Request membership
await courseProfileStore.requestToBeMember(courseId, groupId);

// Cancel membership
await courseProfileStore.requestToBeUnmember(courseId, memberId);
```

## Breaking Changes

**ไม่มี breaking changes** - การปรับปรุงนี้รักษา backward compatibility

## Backend Requirements

### Laravel Controller ต้องส่งข้อมูลเพิ่มเติม:

```php
// ใน Controller method ที่ render หน้า Course
public function show($id)
{
    $course = Course::findOrFail($id);

    return Inertia::render('Learn/Courses/Show', [
        'course' => $course,

        // ⭐ ส่งข้อมูลกลุ่มมาด้วย
        'courseGroups' => $course->groups()
            ->orderBy('name')
            ->get()
            ->map(fn($group) => [
                'id' => $group->id,
                'name' => $group->name,
                'image' => $group->image_url,
                'members_count' => $group->members()->count(), // Optional
            ]),

        'isCourseAdmin' => auth()->user()?->canManageCourse($course),
        'courseMemberOfAuth' => $course->members()
            ->where('user_id', auth()->id())
            ->first(),
    ]);
}
```

### ตัวอย่างการทดสอบว่าข้อมูลส่งมาถูกต้อง:

```vue
<script setup>
import { usePage } from "@inertiajs/vue3";

onMounted(() => {
    console.log("Course Groups:", usePage().props.courseGroups);
    // ควรเห็น array ของ groups พร้อม id, name, image
});
</script>
```

## Dependencies

-   `@iconify/vue` - สำหรับ icons
-   `@inertiajs/vue3` - สำหรับ page props
-   Pinia store (`courseProfile.js`)
-   Optional: Toast notification library (ถ้ามี `window.$toast`)

## Next Steps

1. **เพิ่ม unit tests** สำหรับ membership functions
2. **เพิ่ม custom confirmation modal** แทน browser's native confirm dialog
3. **เพิ่ม animation เมื่อเปลี่ยนสถานะ** จาก "รอการอนุมัติ" เป็น "สมาชิก"
4. **เพิ่ม loading skeleton** สำหรับ initial state
5. **พิจารณาเพิ่ม undo functionality** สำหรับการออกจากสมาชิก

## ไฟล์ที่เกี่ยวข้อง

-   `resources/js/PlearndComponents/learn/courses/CourseProfileCover.vue`
-   `resources/js/stores/courseProfile.js`

## Testing Checklist

-   [ ] ทดสอบการขอเป็นสมาชิกโดยไม่เลือกกลุ่ม
-   [ ] ทดสอบการขอเป็นสมาชิกพร้อมเลือกกลุ่ม
-   [ ] ทดสอบการยกเลิกคำขอ (สถานะรอการอนุมัติ)
-   [ ] ทดสอบการออกจากสมาชิก (สมาชิกที่ได้รับการอนุมัติ)
-   [ ] ทดสอบการปิด dropdown ด้วยการคลิกนอกพื้นที่
-   [ ] ทดสอบการปิด dropdown ด้วย Escape key
-   [ ] ทดสอบการป้องกันการคลิกซ้ำ
-   [ ] ทดสอบ responsive design บน mobile และ desktop
-   [ ] ทดสอบการแสดง error messages
-   [ ] ทดสอบการแสดง success notifications

## สรุป

การปรับปรุงนี้ช่วยเพิ่มประสบการณ์ผู้ใช้ (UX) และความเสถียรของระบบสมัครเข้าร่วมรายวิชา โดยเน้นที่:

-   การป้องกัน edge cases
-   การให้ feedback ที่ชัดเจนแก่ผู้ใช้
-   การปรับปรุง UI/UX ให้ทันสมัยและใช้งานง่าย
-   การจัดการ state อย่างมีประสิทธิภาพ
